import colors from 'tailwindcss/colors'

export default {
  theme: {
    colors: {
      primary: colors.blue,
      danger: colors.red,
      inactive: colors.gray,
      default: colors.slate,
    }
  }
}
